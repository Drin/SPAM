package com.drin.clustering;

import com.drin.clustering.Cluster;
import com.drin.clustering.HCluster;

import org.junit.Test;

import java.util.List;
import java.util.ArrayList;

public class CloseClusterPairTest {
   private static final List<Cluster> TEST_CLUSTERS = new ArrayList<Cluster>() {{
      //TODO there is so much required to make clusters it's ridiculous
      add(new HCluster() { });
   }};

   @Test
   public void correctlyConstructsCloseClusterPairObject() {

   }
}
